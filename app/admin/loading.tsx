export default function AdminLoading() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-[var(--cream)] text-[var(--burgundy)]">
      <p className="text-[10px] font-semibold uppercase tracking-[0.24em]">Loading workspace</p>
    </main>
  );
}